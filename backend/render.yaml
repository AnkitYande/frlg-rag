services:
  - type: web
    name: frlg-rag-api
    runtime: python
    buildCommand: pip install -r api/requirements.txt
    startCommand: gunicorn --chdir api api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # set manually in Render dashboard
    disk:
      name: chroma-data
      mountPath: /opt/render/project/src/data/chroma
      sizeGB: 1
