services:
  - type: web
    name: frlg-rag-api
    runtime: python
    pythonVersion: "3.12"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --bind 0.0.0.0:10000
    envVars:
      - key: GEMINI_API_KEY
        sync: false